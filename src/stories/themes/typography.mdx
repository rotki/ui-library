import listClasses from './typography-classes-list';
import { Meta } from '@storybook/blocks';

<Meta title="Themes/Typography" />

# Typography

<table>
  <tbody>
    <tr>
      <th>Class Name</th>
      <th>Example</th>
    </tr>
    {listClasses.map(({ name, className}) => (
      <tr key={name}>
        <td className="font-bold"><code>{className}</code></td>
        <td className="sb-unstyled"><div className={className}>{name}</div></td>
      </tr>
    ))}
  </tbody>
</table>
