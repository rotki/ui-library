import typographyClasses from '@/consts/typography';
import { Meta } from '@storybook/blocks';

<Meta title="Themes/Typography" />

# Typography

<table>
  <tbody>
    <tr>
      <th>Class Name</th>
      <th>Example</th>
    </tr>
    {typographyClasses.map(({ name, className}) => (
      <tr key={name}>
        <td className="font-bold"><code>{className}</code></td>
        <td className="sb-unstyled"><div className={className}>{name}</div></td>
      </tr>
    ))}
  </tbody>
</table>
